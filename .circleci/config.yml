version: 2.1
jobs:
  android-build-and-test:
    working_directory: ~/dev/ionic-test
    machine:
      image: android:202102-01

    steps:
      - checkout:
          path: ~/dev/ionic-test
      - run:
          name: List file content
          command: |
            ls -l
workflows:
  say-hello-workflow:
    jobs:
      - android-build-and-test
